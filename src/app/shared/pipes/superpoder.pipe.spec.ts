import { SuperpoderPipe } from './superpoder.pipe';

describe('SuperpoderPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperpoderPipe();
    expect(pipe).toBeTruthy();
  });
});
